---
import ThemeToggle from './ThemeToggle.astro';
import LanguageSelector from './LanguageSelector.astro';
import { useTranslations } from '../../i18n/utils';
import type { SupportedLocale } from '../../i18n/ui';

export interface Props {
  locale?: SupportedLocale;
  currentPath?: string;
}

const { locale = 'en', currentPath = Astro.url.pathname } = Astro.props;
const t = useTranslations(locale);

const homePath = `/${locale}/`;
const tools = [
  { key: 'checker', name: t('tools.checker.name'), path: `/${locale}/utm-checker` },
  { key: 'builder', name: t('tools.builder.name'), path: `/${locale}/utm-builder` },
  { key: 'parser', name: t('tools.parser.name'), path: `/${locale}/utm-parser` },
  { key: 'naming', name: t('tools.naming.name'), path: `/${locale}/utm-naming-generator` },
  { key: 'bulk', name: t('tools.bulk.name'), path: `/${locale}/bulk-utm-builder` },
];

const guidesPath = `/${locale}/guides`;
const aboutPath = `/${locale}/about`;
const contactPath = `/${locale}/contact`;
---

<header class="sticky top-0 z-40 w-full border-b border-[var(--border-subtle)] bg-[var(--bg-app)]/70 backdrop-blur-md transition-colors duration-150">
  <div class="mx-auto flex h-14 max-w-5xl items-center justify-between px-4 sm:px-6">
    <!-- Brand Wordmark -->
    <a
      href={homePath}
      class="inline-flex items-center text-sm sm:text-base font-semibold tracking-tight text-[var(--text-primary)] hover:opacity-85 transition-opacity focus-ring rounded"
      aria-label="UTMVerify"
    >
      <span class="tracking-tight font-medium text-[var(--text-primary)]">
        UTM<span class="text-[var(--text-secondary)] font-normal">Verify</span>
      </span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="hidden md:flex items-center gap-7 text-xs sm:text-sm font-medium text-[var(--text-secondary)]">
      <!-- Tools Dropdown -->
      <div class="relative group" id="tools-dropdown-container">
        <button
          type="button"
          class="inline-flex items-center gap-1.5 py-1 text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors focus-ring rounded"
          aria-haspopup="true"
          aria-expanded="false"
          id="tools-menu-button"
        >
          <span>{t('nav.tools')}</span>
          <svg class="w-3.5 h-3.5 text-[var(--text-muted)] transition-transform duration-150 group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>

        <div class="absolute left-0 top-full mt-1.5 w-52 rounded-lg border border-[var(--border-subtle)] bg-[var(--bg-surface)] p-1.5 shadow-lg backdrop-blur-md opacity-0 translate-y-1 invisible group-hover:opacity-100 group-hover:translate-y-0 group-hover:visible transition-all duration-150 z-50">
          {tools.map((tool) => (
            <a
              href={tool.path}
              class={`block rounded-md px-3 py-2 text-xs font-medium transition-colors ${
                currentPath === tool.path
                  ? 'bg-[var(--accent-subtle)] text-[var(--accent)] font-semibold'
                  : 'text-[var(--text-secondary)] hover:bg-[var(--bg-subtle)] hover:text-[var(--text-primary)]'
              }`}
            >
              {tool.name}
            </a>
          ))}
        </div>
      </div>

      <a
        href={guidesPath}
        class={`py-1 transition-colors hover:text-[var(--text-primary)] ${currentPath === guidesPath ? 'text-[var(--text-primary)] font-semibold' : ''}`}
      >
        {t('nav.guides')}
      </a>

      <a
        href={aboutPath}
        class={`py-1 transition-colors hover:text-[var(--text-primary)] ${currentPath === aboutPath ? 'text-[var(--text-primary)] font-semibold' : ''}`}
      >
        {t('nav.about')}
      </a>

      <a
        href={contactPath}
        class={`py-1 transition-colors hover:text-[var(--text-primary)] ${currentPath === contactPath ? 'text-[var(--text-primary)] font-semibold' : ''}`}
      >
        {t('nav.contact')}
      </a>
    </nav>

    <!-- Right Actions: Language Selector, Theme Toggle, Mobile Menu Button -->
    <div class="flex items-center gap-2 sm:gap-2.5">
      <LanguageSelector currentLocale={locale} currentPath={currentPath} />
      <ThemeToggle locale={locale} />

      <button
        type="button"
        id="mobile-menu-btn"
        aria-label={t('nav.menu')}
        class="inline-flex md:hidden items-center justify-center w-9 h-9 rounded-md border border-[var(--border-subtle)] bg-[var(--bg-surface)] text-[var(--text-secondary)] hover:text-[var(--text-primary)] hover:border-[var(--border-strong)] transition-colors focus-ring cursor-pointer"
      >
        <svg id="hamburger-icon" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
        <svg id="close-icon" class="w-4 h-4 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile Navigation Drawer -->
  <div
    id="mobile-menu"
    class="hidden md:hidden border-t border-[var(--border-subtle)] bg-[var(--bg-app)] px-4 py-4 transition-all"
  >
    <div class="flex flex-col space-y-2 text-sm">
      <div class="pb-1 text-xs font-semibold uppercase tracking-wider text-[var(--text-muted)]">
        {t('nav.tools')}
      </div>
      {tools.map((tool) => (
        <a
          href={tool.path}
          class={`flex items-center justify-between rounded-md px-3 py-2 text-sm transition-colors ${
            currentPath === tool.path
              ? 'bg-[var(--accent-subtle)] text-[var(--accent)] font-semibold'
              : 'text-[var(--text-secondary)] hover:bg-[var(--bg-surface)] hover:text-[var(--text-primary)]'
          }`}
        >
          <span>{tool.name}</span>
        </a>
      ))}
      <div class="my-2 border-t border-[var(--border-subtle)]"></div>
      <a
        href={guidesPath}
        class="px-3 py-2 text-sm text-[var(--text-secondary)] hover:bg-[var(--bg-surface)] hover:text-[var(--text-primary)] rounded-md transition-colors"
      >
        {t('nav.guides')}
      </a>
      <a
        href={aboutPath}
        class="px-3 py-2 text-sm text-[var(--text-secondary)] hover:bg-[var(--bg-surface)] hover:text-[var(--text-primary)] rounded-md transition-colors"
      >
        {t('nav.about')}
      </a>
      <a
        href={contactPath}
        class="px-3 py-2 text-sm text-[var(--text-secondary)] hover:bg-[var(--bg-surface)] hover:text-[var(--text-primary)] rounded-md transition-colors"
      >
        {t('nav.contact')}
      </a>
    </div>
  </div>
</header>

<script>
  function initMobileMenu() {
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');
    const burger = document.getElementById('hamburger-icon');
    const close = document.getElementById('close-icon');

    if (!btn || !menu || !burger || !close) return;

    btn.addEventListener('click', () => {
      const isHidden = menu.classList.contains('hidden');
      if (isHidden) {
        menu.classList.remove('hidden');
        burger.classList.add('hidden');
        close.classList.remove('hidden');
      } else {
        menu.classList.add('hidden');
        burger.classList.remove('hidden');
        close.classList.add('hidden');
      }
    });
  }

  initMobileMenu();
  document.addEventListener('astro:after-swap', initMobileMenu);
</script>
